<UserControl x:Class="Supermarketmanagement.PresentationLayer.UserControls.AddMultilpleSaleBillUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Supermarketmanagement.PresentationLayer.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="20" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <Button x:Name="ButtonAddmore" Content="Thêm hóa đơn +" HorizontalAlignment="Left"  Click="MenuAddSaleBill_Click" Width="100" />
            <Button Margin="5 0 0 0"  Content="Xóa hết -" HorizontalAlignment="Left"  Click="ClearAll_Click" Width="100" />

        </StackPanel>
        <TabControl Grid.Row="1" x:Name="TabControl_AddSaleBills">

        </TabControl>
    </Grid>
</UserControl>
